{{>header}}

import type { ApiRequestOptions, Resolver, Headers, ErrorFactory } from './ApiRequestOptions';
import type { ApiResult } from './ApiResult';

export type ApiOptions = {
    baseUrl: string;
    version: string;
    withCredentials: boolean;
    token?: string | Resolver<string>;
    username?: string | Resolver<string>;
    password?: string | Resolver<string>;
    defaultHeaders?: Headers | Resolver<Headers>;
    errorFactory?: ErrorFactory;
    getResponseBody?: (options: ApiRequestOptions, response: Response) => Promise<any>;
    catchErrors?: (options: ApiRequestOptions, result: ApiResult) => void;
}
